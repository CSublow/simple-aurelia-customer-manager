export class Customer {
    /* Create a class called Customer, create some fields (name, email, phone) */
    constructor(name, email, phone) {
            this.name = name;
            this.email = email;
            this.phone = phone;
    }
}